# Force Plymouth to start before block devices and filesystems engage
MODULES=(i915 amdgpu nouveau)
BINARIES=()
FILES=()
HOOKS=(base udev plymouth autodetect modconf kms block filesystems keyboard fsck)
